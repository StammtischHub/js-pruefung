services:
  scanner-mock:
    image: ghcr.io/thst71/pdfclassifier-api-mock-scanner-mock:v1_1_0
    pull_policy: always
    restart: unless-stopped
    volumes:
      - ./data/scanner:/inbox

  pdfclassifier-api-mock:
    image: ghcr.io/thst71/pdfclassifier-api-mock:v1_1_0
    pull_policy: always
    restart: unless-stopped
    ports:
      - "8080:8080"

  backend:
    build:
      context: .
      dockerfile: apps/bff/Dockerfile
    restart: unless-stopped
    ports:
      - "4000:4000"
    depends_on:
      - scanner-mock
      - pdfclassifier-api-mock
    volumes:
      - ./data:/data
    environment:
      NODE_ENV: production
      PORT: 4000

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
    restart: unless-stopped
    ports:
      - "5173:5173"
    depends_on:
      - backend
